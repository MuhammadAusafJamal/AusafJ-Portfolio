{
  "name": "@ausaf/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod definitions for every content and request shape. Imported by the CLI (authoring), the build (validation), and the API (boundary parsing).",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
