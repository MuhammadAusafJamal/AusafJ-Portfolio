/**
 * Shared between the page's `<meta name="description">` and its share card, which
 * are the same sentence read in two places and should stay that way.
 */
export const PROJECTS_DESCRIPTION = 'Case studies for the work worth reading about at length.';
